export interface INamespace {
  name: string
  namespace: string
  replicas: number
  age: string
  status: boolean
}
export interface INode {
  name: string
  namespace: string
  replicas: number
  age: string
  status: boolean
}
